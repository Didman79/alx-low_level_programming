#include "main.h"
/**
 * _strlen- to evaluate the length of a string
 * @s: string to determine length
 * return: length
 *
 */


int _strlen(char *s)
{
	int n = 0;

	while (s[i] != '\0)
	{
		n++;
	}

	return (n);
}
